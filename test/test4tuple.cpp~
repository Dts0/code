#include<iostream>
#include<stdlib.h>
#include<tuple>

using namespace std;
int main(){
	tuple<int,string,string> tp;
	cin>>get<0>(tp)>>get<1>(tp)>>get<2>(tp);
	cout<<get<0>(tp)<<","<<get<1>(tp)<<","<<get<2>(tp)<<endl;
	cout<<tuple_size<tuple<int,string,string>>::value<<endl;

	return 0;
}
